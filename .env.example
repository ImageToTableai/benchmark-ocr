# Environment — EXAMPLE ONLY
# Copy to `.env` and fill in your real values. `.env` is gitignored.
#
#   cp .env.example .env

# HuggingFace token — only needed to download gated datasets (e.g. DocILE).
# Leave empty for the public SROIE/CORD core. Never commit a real token.
export HF_TOKEN=
